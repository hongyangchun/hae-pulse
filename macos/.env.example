# HAE Pulse — macOS 读 key
#
# 从 Health Auto Export 拿到的只读 API key，用来查询 hae.qiaclass.com。
# 与 iPhone 上那个 MCP 的 Bearer token 不是同一个东西。
#
# 用法：cp .env.example .env 然后填值。这个文件是「项目独立凭证」，
# 只有经 render.py（SwiftBar 插件）取数时生效，且优先级高于 ~/.hermes/.env。
# 详见 README 的「配置」一节。
#
# 想让 macOS 和 Omarchy 共用同一把 key，就别建这个文件，
# 直接把 key 追加到 ~/.hermes/.env（collector.py 的原生位置）。
#
# .env 已在 .gitignore 中，不会被提交。

HAE_READ_KEY=
